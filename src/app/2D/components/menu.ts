// import { createFromConfig } from 'as-wc-lib/src/index';
// import { config } from '../configurations/main menu';

export class Menu {
  /**
   *
   */
  constructor() {
    // createFromConfig(config, document.body);
  }
}
