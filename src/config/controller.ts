import { arrowUp, arrowDown, arrowLeft, arrowRight, W, A, S, D, SPACE, ENTER } from './keyboard-codes';

export const forwardKeyboardKeys: Array<string> = [arrowUp, W];
export const backwardKeyboardKeys: Array<string> = [arrowDown, S];
export const leftKeyboardKeys: Array<string> = [arrowLeft, A];
export const rightKeyboardKeys: Array<string> = [arrowRight, D];
export const runKeyboardKeys: Array<string> = [SPACE];
export const interactKeyboardKeys: Array<string> = [ENTER];
